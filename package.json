{
  "name": "hades-telegram-utils",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc -p ./tsconfig.build.json",
    "start": "node dist/index.js",
    "start:dev": "nodemon --watch 'src/**' --ext 'ts,json' --ignore 'src/**/*.spec.ts' --exec 'ts-node src/index.ts'"
  },
  "dependencies": {
    "airgram": "^4.0.0",
    "axios": "^0.21.1",
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "prettier": "^2.2.1",
    "ramda": "^0.27.1",
    "rimraf": "^3.0.2",
    "translate": "^1.2.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "devDependencies": {
    "@types/node": "^15.0.2",
    "@types/ramda": "^0.27.40"
  }
}
